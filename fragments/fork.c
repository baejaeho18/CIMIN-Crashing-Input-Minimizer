#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char* argv[]){
    pid_t = pid;

    while(1){
        if((pid = fork()) < 0){
            fprintf(stderr, "fork error");
        }


    }


    return 0;
}